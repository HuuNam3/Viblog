export interface Video {
  id: number;
  videoUrl: string;
  title: string;  // Added title field to match validation
  username: string;
  caption: string;
  likes: string;
  userAvatar: string;
} 